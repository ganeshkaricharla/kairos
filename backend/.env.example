# MongoDB Configuration
MONGODB_URL=mongodb://localhost:27017
DATABASE_NAME=kairos

# OpenRouter API Configuration
# Get your API key from: https://openrouter.ai/keys
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Google OAuth Configuration
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here.apps.googleusercontent.com

# JWT Configuration
# Generate a secure random string for JWT_SECRET_KEY (e.g., using: openssl rand -hex 32)
JWT_SECRET_KEY=your_super_secret_jwt_key_min_32_characters_long
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=10080  # 7 days (7 * 24 * 60)

# Encryption Configuration
# For encrypting user API keys at rest
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=your_base64_encryption_key_here

# Development Notes:
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit the .env file to git
# 3. For local development, start MongoDB with: docker-compose up -d
# 4. Make sure your Google OAuth redirect URI includes: http://localhost:5173
